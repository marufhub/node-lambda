{
  "name": "dap-lambda",
  "version": "1.0.0",
  "description": "This is a general API for Output datasets of DAP(Data Analytics Platform). This is build on AWS API Gateway and Lambda(NodeJS). API Gateway handle the HTTP request, response and invoke Lambda function for processing. Lambda function interact with SSO and database to perform the operation.",
  "main": "index.js",
  "scripts": {
	"prestart": "npm install",
	"start": "node index.js"
  },  
  "keywords": [
    "swagger"
  ],
  "license": "Apache-2.0",
  "private": true,
  "dependencies": {
    "connect": "^3.2.0",
    "js-yaml": "^3.3.0",
    "swagger-tools": "0.10.1"
  }
}
